---

tlp_packages:
  - tlp                   # PowerManagement fot thinkpad
  - acpi-call-dkms        # Kernel module
  - tp-smapi-dkms         # Kernel module
tlp_services:
  - tlp
tlp_triggered_services: []
tlp_repo:
  - repo: ppa:linrunner/tlp
    codename: "{{ ansible_distribution_release }}"
# vim: set ts=2 sw=2:
